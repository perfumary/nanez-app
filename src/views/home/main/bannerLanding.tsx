import React from "react";
import {Text} from "react-native";
import {SafeAreaView} from "react-native-safe-area-context";

const BannerView: React.FC = () => {
  return (
    <SafeAreaView>
      <Text>banner landing</Text>
    </SafeAreaView>
  );
};

export default BannerView;
